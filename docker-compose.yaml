version: "3.8"

services:
  db:
    image: postgres:15
    env_file:
      - .env
    ports:
      - "5437:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  email:
    image: mailhog/mailhog
    ports:
      - '1025:1025'
      - "8025:8025"

volumes:
  postgres_data: